/*
we will learn about enumerations also called as enums
also help us to define a type
but by enumerating its different variants

we will learn how an enum can encode meaninf along ith data
next we will learn about Option enum => expresses that the value can be either something or nothing

then pattern matchin fin the match exoression
to run different code for different values of an enum

then learn about if let construct
to handle enums in the code


An enumeration is a collection of things that are named or listed in order.
 */

fn main() {
    println!("Hello, world!");
}
